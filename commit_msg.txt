🧹 [code health improvement] Remove duplicate backtick conditional branch

🎯 What: Removed duplicate `elif mode == 'backtick' and ch == '\`': mode = None` conditional branch in `sql_compare.py`'s `_tokenize_from_clause_body` function. Added a unit test `TestTokenizeFromClauseBody` to ensure proper backtick handling is tested.
💡 Why: The duplicate conditional branch was redundant code that served no purpose and cluttered the string parsing state machine. Removing it simplifies the code and improves readability and maintainability.
✅ Verification: Ran the test suite. Wrote and verified a new unit test for backtick parsing inside `_tokenize_from_clause_body` which successfully handles backticks. Verified no regressions in functionality.
✨ Result: Cleaner string parsing logic in the FROM clause tokenizer with zero performance or functional regressions, while test coverage for the tokenizer's backtick handling has been added.
